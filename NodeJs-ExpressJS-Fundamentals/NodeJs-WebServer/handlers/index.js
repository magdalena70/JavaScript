const homePageHandler = require('./home-page')
const faviconHandler = require('./favicon')
const filesHandler = require('./file-handler')

module.exports = [
    homePageHandler,
    faviconHandler,
    filesHandler
]